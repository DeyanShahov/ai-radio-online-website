{
  "scripts": {
    "dev": "netlify dev",
    "dev:env": "dotenv -e .env -- netlify dev",
    "build": "echo 'No build step required'"
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "node-fetch": "^3.3.2"
  },
  "devDependencies": {
    "dotenv-cli": "^11.0.0"
  }
}
