# AI Radio Online - Netlify Frontend

Този проект съдържа уебсайта за AI Radio Online, който се хоства в Netlify и служи като сигурна входна точка за достъп до радиото.

## 🚀 Функционалност

- **Изчистен и минималистичен дизайн** с модерен UI
- **Генериране на HMAC-подписани токени** чрез Netlify serverless функции
- **Автоматично пренасочване** към AI Radio сървъра с валидни оторизационни параметри
- **Анонимно потребителско изживяване** - не се изисква регистрация

## 📁 Структура на проекта

```
ai-radio-netlify-frontend/
├── index.html          # Основна страница
├── style.css           # CSS стилове
├── script.js           # Клиентска JavaScript логика
├── netlify.toml        # Конфигурация за Netlify
├── netlify/
│   └── functions/
│       └── generate-token.js  # Serverless функция за токени
└── README.md           # Тази документация
```

## 🔧 Настройка и деплоймънт

### 1. Локално тестване

```bash
# Инсталирайте Netlify CLI (ако нямате)
npm install -g netlify-cli

# Стартирайте локалния сървър
netlify dev
```

### 2. Деплоймънт в Netlify

1. **Създайте нов сайт в Netlify:**
   - Отидете на [netlify.com](https://netlify.com)
   - Свържете GitHub хранилището или качете файловете ръчно

2. **Настройте променливите на средата:**
   - В Netlify Dashboard отидете на Site settings > Build & deploy > Environment variables
   - Добавете `TOKEN_SECRET` със силна, случайна стойност (минимум 32 символа)
   - **Важно:** Тази стойност трябва да е същата като в C# сървъра (`appsettings.json`)

3. **Деплойнете:**
   - Netlify автоматично ще разпознае `netlify.toml` и ще конфигурира сайта
   - Функциите ще бъдат достъпни на `/.netlify/functions/generate-token`

### 3. Конфигурация на C# сървъра

Уверете се, че в `appsettings.json` на C# приложението имате:

```json
{
  "AuthSettings": {
    "TokenSecret": "ВАШИЯТ_ТАЕН_КЛЮЧ_ТУК"
  }
}
```

**Забележка:** `TokenSecret` в Netlify и C# сървъра трябва да са идентични!

## 🔐 Сигурност

- **HMAC-SHA256 подписване** на токените
- **Кратък срок на валидност** (60 секунди)
- **CORS защита** за предотвратяване на неоторизиран достъп
- **HTTPS принудително** в Netlify

## 🎯 Как работи

1. **Потребителят отваря сайта** и кликва "Стартирай AI Радио"
2. **JavaScript кодът** генерира уникален потребителски ID и извиква Netlify функцията
3. **Netlify функцията** генерира HMAC-подписан токен със срок на валидност
4. **Браузърът се пренасочва** към AI Radio сървъра с токена като query параметър
5. **C# сървърът** валидира токена и позволява достъпа до стрийма

## 🛠️ Разработка

### Промяна на радио сървъра URL

В `script.js` променете `this.radioServerUrl`:

```javascript
this.radioServerUrl = 'https://your-radio-server.com';
```

### Промяна на канала по подразбиране

В `script.js` променете `this.defaultChannel`:

```javascript
this.defaultChannel = 'your-channel-name';
```

## 📝 Бележки

- Сайтът е проектиран да бъде напълно статичен и да работи без допълнителни зависимости
- Всички стилове са responsive и работят добре на мобилни устройства
- Netlify функциите се изпълняват само при генериране на токени, не при стрийминга

## 🤝 Принос

Моля, спазвайте насоките в `.clinerules/` за Git операции и код стил.
